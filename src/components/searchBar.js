import React from 'react';
import '../App.css';

const SearchBar = () => {
    return (
        <div className="search">
            <input class="bar" type="text" placeholder="Search for a topic" />
        </div>
    );
};

export default SearchBar;